{
  "name": "@infra/config",
  "version": "1.0.0",
  "private": true,
  "description": "Shared configuration, types, and utilities for infrastructure",
  "main": "src/index.ts",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.7.3"
  }
}