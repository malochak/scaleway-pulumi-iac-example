{
  "name": "@scaleway-example/infra",
  "version": "1.0.0",
  "private": true,
  "description": "Infrastructure as code for Scaleway",
  "engines": {
    "node": ">=24.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "build": "pnpm -r build"
  }
}